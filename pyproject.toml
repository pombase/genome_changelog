[tool.poetry]
name = "genome_changelog"
version = "0.1.0"
description = ""
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.10"
biopython = "^1.79"

[tool.poetry.dev-dependencies]
ipython = "^8.5.0"
ipykernel = "^6.16.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
